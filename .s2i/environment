# Variables S2I basadas en la configuración exitosa de meteorología

# Configuración de Maven para Fast JAR (como en meteorología)
MAVEN_ARGS=clean package -DskipTests=true -Dquarkus.package.type=fast-jar

# Configuración del artefacto para Fast JAR
ARTIFACT_DIR=target/quarkus-app

# Variables de entorno adicionales
QUARKUS_HTTP_HOST=0.0.0.0
QUARKUS_HTTP_PORT=8080
LANG=C.UTF-8
LC_ALL=C.UTF-8

# Optimizaciones JVM
JAVA_OPTIONS=-Xms128m -Xmx512m -XX:+UseParallelGC
JAVA_OPTS_APPEND=-Dquarkus.http.host=0.0.0.0 -Djava.util.logging.manager=org.jboss.logmanager.LogManager